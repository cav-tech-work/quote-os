"use client";
import { useEffect, useMemo, useState } from "react";
import { AppSidebar } from "./components/app-sidebar";

type Side = "TO_CLIENT" | "TO_VENDOR";
type Offering = { id: string; code: string; name: string; canonicalCode: string | null; canonicalName: string | null; quantityBasis: "COUNT" | "AREA_LW" | "AREA_LH" | "LINEAR" | "VOLUME" | "FIXED"; billingUnit: string; durationPolicyCode: string; durationPolicyMode: string; priceId: string; unitRatePaise: number };
type Configuration = { quantity: string; length?: { value: string; unit: "FT" | "M" }; width?: { value: string; unit: "FT" | "M" }; height?: { value: string; unit: "FT" | "M" } };
type Preview = { billableQuantity: string; usageDays: string; chargeUnits: string; finalAmountPaise: number; pricingState: string };
type Line = { offering: Offering; configuration: Configuration; usageDays: string; discountPercent: number; preview: Preview | null; error: string };
const money = new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 2 });

function initialConfiguration(basis: Offering["quantityBasis"]): Configuration {
  const result: Configuration = { quantity: "1" };
  if (["AREA_LW", "AREA_LH", "LINEAR", "VOLUME"].includes(basis)) result.length = { value: "10", unit: "FT" };
  if (["AREA_LW", "VOLUME"].includes(basis)) result.width = { value: "10", unit: "FT" };
  if (["AREA_LH", "VOLUME"].includes(basis)) result.height = { value: "10", unit: "FT" };
  return result;
}

export function QuoteWorkspace() {
  const [catalogue, setCatalogue] = useState<Offering[]>([]); const [type, setType] = useState<"CLIENT" | "VENDOR">("CLIENT"); const [lines, setLines] = useState<Line[]>([]); const [search, setSearch] = useState(""); const [catalogueOpen, setCatalogueOpen] = useState(false); const [eventDays, setEventDays] = useState(4); const [tax, setTax] = useState(18); const [company, setCompany] = useState(""); const [project, setProject] = useState(""); const [venue, setVenue] = useState(""); const [city, setCity] = useState(""); const [status, setStatus] = useState("");
  const side: Side = type === "CLIENT" ? "TO_CLIENT" : "TO_VENDOR";
  useEffect(() => { setLines([]); fetch(`/api/normalized-offerings?side=${side}`).then(async (response) => { if (!response.ok) throw new Error(); setCatalogue((await response.json()).offerings); }).catch(() => setStatus("Could not load the normalized catalogue.")); }, [side]);
  const results = catalogue.filter((item) => `${item.code} ${item.name} ${item.canonicalCode ?? ""} ${item.canonicalName ?? ""}`.toLowerCase().includes(search.toLowerCase()));
  const totals = useMemo(() => { const sub = lines.reduce((total, line) => total + (line.preview?.finalAmountPaise ?? 0), 0); const discount = lines.reduce((total, line) => total + Math.round((line.preview?.finalAmountPaise ?? 0) * line.discountPercent / 100), 0); const taxTotal = Math.round((sub - discount) * tax / 100); return { sub, discount, taxTotal, total: sub - discount + taxTotal }; }, [lines, tax]);
  const update = (index: number, change: (line: Line) => Line) => setLines((old) => old.map((line, i) => i === index ? change(line) : line));
  const setDimension = (index: number, field: "length" | "width" | "height", key: "value" | "unit", value: string) => update(index, (line) => ({ ...line, preview: null, ["configuration"]: { ...line.configuration, [field]: { ...(line.configuration[field] ?? { value: "", unit: "FT" }), [key]: value } } as Line["configuration"] }));
  async function preview(index: number) { const line = lines[index]; const response = await fetch("/api/normalized-offerings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ commercialOfferingId: line.offering.id, side, configuration: line.configuration, usageDays: line.usageDays || String(eventDays), expectedPriceId: line.offering.priceId }) }); const result = await response.json(); update(index, (current) => response.ok ? { ...current, preview: result.calculation, error: "" } : { ...current, preview: null, error: result.error ?? result.code ?? "Preview unavailable" }); }
  async function saveDraft() { setStatus(""); if (company.trim().length < 2) return setStatus("Enter a company name with at least 2 characters."); if (!lines.length) return setStatus("Add at least one normalized offering."); const response = await fetch("/api/quotes", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ mode: "NORMALIZED", type, company, project, venue, city, eventDays, taxPercentage: tax, lines: lines.map((line) => ({ commercialOfferingId: line.offering.id, configuration: line.configuration, usageDays: line.usageDays || undefined, expectedPriceId: line.offering.priceId, discountPercent: line.discountPercent })) }) }); const result = await response.json().catch(() => ({})); setStatus(response.ok ? `Normalized draft ${result.number} saved.` : typeof result.error === "string" ? `${result.code ?? "ERROR"}: ${result.error}` : result.code ?? "Could not save the draft."); }
  return <main className="app"><AppSidebar /><section className="content"><header><div><small>NORMALIZED QUOTATION / DRAFT</small><h1>Create an ordinary quote</h1><p>Only current, ordinary, quote-ready commercial offerings are selectable.</p></div><button className="primary" onClick={saveDraft}>Save immutable revision</button></header>{status && <p className="notice">{status}</p>}<p className="steps"><span>1</span> Details <span>2</span> Configure and preview <span>3</span> Server-validated snapshot</p><div className="grid"><article><h2>Quote details</h2><p className="toggle"><button className={type === "CLIENT" ? "chosen" : ""} onClick={() => setType("CLIENT")}>Offer to client</button><button className={type === "VENDOR" ? "chosen" : ""} onClick={() => setType("VENDOR")}>Offer to vendor</button></p><div className="fields"><label>Company<input value={company} onChange={(e) => setCompany(e.target.value)} /></label><label>Project<input value={project} onChange={(e) => setProject(e.target.value)} /></label><label>Venue<input value={venue} onChange={(e) => setVenue(e.target.value)} /></label><label>City<input value={city} onChange={(e) => setCity(e.target.value)} /></label><label>Event days<input type="number" min="1" step="1" value={eventDays} onChange={(e) => setEventDays(Math.max(1, Number(e.target.value) || 1))} /></label></div></article><article className="total"><small>{type} QUOTE PREVIEW</small><strong>{money.format(totals.total / 100)}</strong><p>Subtotal <b>{money.format(totals.sub / 100)}</b></p><p>Discount <b>−{money.format(totals.discount / 100)}</b></p><p>GST ({tax}%) <b>{money.format(totals.taxTotal / 100)}</b></p><label>Tax rate<input type="number" value={tax} onChange={(e) => setTax(Number(e.target.value))} /></label></article></div><article><div className="sectionTitle"><div><small>NORMALIZED CATALOGUE</small><h2>Ordinary offerings</h2></div><em>{catalogue.length} selectable · {lines.length} selected</em></div><div className="search"><input placeholder="Search offering or canonical code/name" value={search} onFocus={() => setCatalogueOpen(true)} onChange={(e) => { setSearch(e.target.value); setCatalogueOpen(true); }} onBlur={() => window.setTimeout(() => setCatalogueOpen(false), 150)} />{catalogueOpen && <div>{results.slice(0, 12).map((item) => <button key={item.id} onMouseDown={(e) => e.preventDefault()} onClick={() => { setLines((old) => [...old, { offering: item, configuration: initialConfiguration(item.quantityBasis), usageDays: "", discountPercent: 0, preview: null, error: "" }]); setSearch(""); setCatalogueOpen(false); }}>{item.name}<small>{item.code} · {item.canonicalName ?? "—"} · {item.quantityBasis} · {item.durationPolicyCode}</small></button>)}</div>}</div>{lines.map((line, index) => <div className="normalizedLine" key={`${line.offering.id}-${index}`}><header><span><b>{line.offering.name}</b><small>{line.offering.code} · {line.offering.quantityBasis} · {line.offering.billingUnit} · {line.offering.durationPolicyCode}</small></span><button className="remove" onClick={() => setLines((old) => old.filter((_, i) => i !== index))}>×</button></header><div className="configurationGrid"><label>Quantity<input value={line.configuration.quantity} onChange={(e) => update(index, (old) => ({ ...old, preview: null, configuration: { ...old.configuration, quantity: e.target.value } }))} /></label>{(["length", "width", "height"] as const).filter((field) => line.configuration[field]).map((field) => <label key={field}>{field}<div className="measurement"><input value={line.configuration[field]!.value} onChange={(e) => setDimension(index, field, "value", e.target.value)} /><select value={line.configuration[field]!.unit} onChange={(e) => setDimension(index, field, "unit", e.target.value)}><option>FT</option><option>M</option></select></div></label>)}<label>Usage days override<input placeholder={`Default ${eventDays}`} value={line.usageDays} onChange={(e) => update(index, (old) => ({ ...old, preview: null, usageDays: e.target.value }))} /></label><label>Discount %<input type="number" min="0" max="100" value={line.discountPercent} onChange={(e) => update(index, (old) => ({ ...old, discountPercent: Number(e.target.value) }))} /></label><button onClick={() => void preview(index)}>Preview</button></div>{line.error && <p className="lineError">{line.error}</p>}{line.preview && <p className="previewResult">Billable {line.preview.billableQuantity} {line.offering.billingUnit} · usage {line.preview.usageDays} days · {line.preview.chargeUnits} charge units · <b>{money.format(line.preview.finalAmountPaise / 100)}</b></p>}</div>)}</article></section></main>;
}
